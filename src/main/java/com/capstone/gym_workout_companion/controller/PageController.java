package com.capstone.gym_workout_companion.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;

@Controller
public class PageController {

    // Home Page
   /* @GetMapping("/")
    public String home() {
        return "index";  // This corresponds to index.html in the templates folder
    }

    // Login Page
    @GetMapping("/login")
    public String login() {
        return "login";  // This corresponds to login.html in the templates folder
    }

    // Profile Page
    @GetMapping("/profile")
    public String profile() {
        return "profile";  // This corresponds to profile.html in the templates folder
    }

    // Signup Page (for new user registration)
    @GetMapping("/signup")
    public String signup() {
        return "signup";  // This corresponds to signup.html in the templates folder
    }

    // Dashboard Page (could be a user dashboard, admin dashboard, etc.)
    @GetMapping("/dashboard")
    public String dashboard() {
        return "dashboard";  // This corresponds to dashboard.html in the templates folder
    }

    // Settings Page (for user settings, preferences)
    @GetMapping("/settings")
    public String settings() {
        return "settings";  // This corresponds to settings.html in the templates folder
    }

    // About Us Page
    @GetMapping("/about")
    public String about() {
        return "about";  // This corresponds to about.html in the templates folder
    }

    // Contact Page
    @GetMapping("/contact")
    public String contact() {
        return "contact";  // This corresponds to contact.html in the templates folder
    }

    // Help Page (FAQ or support page)
    @GetMapping("/help")
    public String help() {
        return "help";  // This corresponds to help.html in the templates folder
    }

    // Terms of Service Page
    @GetMapping("/terms")
    public String terms() {
        return "terms";  // This corresponds to terms.html in the templates folder
    }

    // Privacy Policy Page
    @GetMapping("/privacy")
    public String privacy() {
        return "privacy";  // This corresponds to privacy.html in the templates folder
    }

    // Logout Page (if you have a custom logout page)
    @GetMapping("/logout")
    public String logout() {
        return "logout";  // This corresponds to logout.html in the templates folder
    }

    // Error Page (for 404 or other errors)
    @GetMapping("/error")
    public String error() {
        return "error";  // This corresponds to error.html in the templates folder
    }*/
}